import type { IndexItem } from "../../core/Indexer";
export declare function isReferenceString(str: string): boolean;
export declare function referencesEqual(a: any, b: any): boolean;
export declare function findReferenceIndex(item: IndexItem, arr?: any[]): number;
